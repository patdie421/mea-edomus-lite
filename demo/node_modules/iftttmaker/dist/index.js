'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _https = require('https');

var _https2 = _interopRequireDefault(_https);

var _url = require('url');

var _url2 = _interopRequireDefault(_url);

var _querystring = require('querystring');

var _querystring2 = _interopRequireDefault(_querystring);

var _httpsProxyAgent = require('https-proxy-agent');

var _httpsProxyAgent2 = _interopRequireDefault(_httpsProxyAgent);

var _parseArgs2 = require('./parseArgs');

var _parseArgs3 = _interopRequireDefault(_parseArgs2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var IFTTTMaker = function () {
  function IFTTTMaker(apiKey) {
    _classCallCheck(this, IFTTTMaker);

    this.apiKey = apiKey;
  }

  _createClass(IFTTTMaker, [{
    key: 'setProxy',
    value: function setProxy(proxy) {
      this.proxy = proxy;
    }
  }, {
    key: 'send',
    value: function send() {
      var _this = this;

      var _parseArgs = _parseArgs3.default.apply(undefined, arguments),
          event = _parseArgs.event,
          values = _parseArgs.values,
          callback = _parseArgs.callback;

      return new Promise(function (resolve, reject) {
        var endpoint = 'https://maker.ifttt.com';
        endpoint += '/trigger/' + _querystring2.default.escape(event) + '/with/key/' + _querystring2.default.escape(_this.apiKey);
        endpoint += '?' + _querystring2.default.stringify(values);

        var options = _url2.default.parse(endpoint);

        if (_this.proxy) {
          options.agent = new _httpsProxyAgent2.default(_this.proxy);
        }

        _https2.default.get(options, function (_ref) {
          var statusCode = _ref.statusCode;

          if (!statusCode || statusCode !== 200) {
            var err = 'Bad response code: ' + statusCode;
            reject(err);
            callback(err);
          } else {
            resolve();
            callback(false);
          }
        }).on('error', function (err) {
          reject(err);
          callback(err);
        });
      });
    }
  }]);

  return IFTTTMaker;
}();

module.exports = function (apiKey) {
  return new IFTTTMaker(apiKey);
};